# --- Versiones ---
TRIGGER_IMAGE_TAG=v4.0.0-v4-beta.27
DOCKER_PROXY_IMAGE_TAG=latest

# --- Conexión a la Webapp (tu dominio público de la webapp) ---
TRIGGER_API_URL=https://trigger.example.com
OTEL_EXPORTER_OTLP_ENDPOINT=https://trigger.example.com/otel

# --- Autenticación del Worker ---
# Pega aquí el token que sale en los logs de la Webapp (se muestra solo una vez)
TRIGGER_WORKER_TOKEN=
# Debe coincidir con el de la Webapp
MANAGED_WORKER_SECRET=

# --- Docker runner / Registry (si usas imágenes privadas en los runs) ---
DOCKER_REGISTRY_URL=
DOCKER_REGISTRY_USERNAME=
DOCKER_REGISTRY_PASSWORD=

# --- Ajustes opcionales del supervisor ---
DEBUG=1
ENFORCE_MACHINE_PRESETS=1
TRIGGER_DEQUEUE_INTERVAL_MS=1000
# DOCKER_AUTOREMOVE_EXITED_CONTAINERS=0
# DOCKER_RUNNER_NETWORKS=supervisor
